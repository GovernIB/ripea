<?xml version="1.0" encoding="UTF-8"?>

<datasources>
 <no-tx-datasource>
    <jndi-name>es.caib.seycon.db.wl</jndi-name>
    <connection-url>${sy_db_url}</connection-url>
    <driver-class>${sy_db_driver}</driver-class>
    <user-name>${sy_db_username}</user-name>
    <password>${sy_db_password}</password>
  </no-tx-datasource>
</datasources>

